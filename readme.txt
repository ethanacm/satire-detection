To run our code:

cd into this directory and run the command "python3 perceptron.py"

This does all of the code except for the bayes classifier, and will take 10-15 minutes to run

To run the bayes classifier, run "python3 bayesline.py"